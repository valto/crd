# Statement Provenance: GitHub Pull Request Examples

Covers both `open-pull-request.md` and `merge-pull-request.md`. All source fetches were performed 2026-08-26. Where GitHub's documentation site rendered content dynamically (client-side) and a plain WebFetch could not retrieve it, the underlying page's embedded `__NEXT_DATA__` JSON (a structured, first-party data payload GitHub's own docs site serves inside the HTML it returns) was parsed directly via `curl` + `python3` instead of being paraphrased by a summarizing model — this is noted per row as "structured docs data" and is a more precise source than a prose summary, not a third-party source.

| Statement | Semantic class | Evidence status | Source / note |
|---|---|---|---|
| `POST /repos/{owner}/{repo}/pulls` — required `head`, `base`; `title` required unless `issue`; optional `body`, `head_repo`, `maintainer_can_modify`, `draft`, `issue` | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data (`restOperations[title="Create a pull request"].bodyParameters`) |
| Create-PR write-access requirement: "you must have write access to the head or the source branch... member of the organization" | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `descriptionHTML` |
| Draft PR plan availability wording | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `descriptionHTML` |
| "This endpoint triggers notifications... may result in secondary rate limiting" | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `descriptionHTML` (Create and Merge operations both carry this sentence) |
| `application/vnd.github.raw+json` "is the default if you do not pass any specific media type" | recommended default (source's own wording) | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `descriptionHTML` |
| Create-PR response codes 201/403/422 | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `statusCodes` |
| `PATCH /repos/{owner}/{repo}/pulls/{pull_number}` — `title`, `body`, `state` (`open`/`closed`), `base`, `maintainer_can_modify`; "You cannot update the base branch on a pull request to point to another repository" | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `restOperations[title="Update a pull request"]` |
| `PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge` — optional `commit_title`, `commit_message`, `sha`, `merge_method` (enum `merge`/`squash`/`rebase`) | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls#merge-a-pull-request — structured docs data, `restOperations[title="Merge a pull request"].bodyParameters` |
| Merge response codes and exact wording: 200 "if merge was successful"; 403 Forbidden; 404 Not Found; 405 "Method Not Allowed if merge cannot be performed"; 409 "Conflict if sha was provided and pull request head did not match"; 422 "Validation failed, or the endpoint has been spammed." | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls#merge-a-pull-request — structured docs data, `statusCodes` |
| Example 200 merge response body `{"sha": "...", "merged": true, "message": "Pull Request successfully merged"}` | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls#merge-a-pull-request — structured docs data, `codeExamples[].response.example` |
| No documented default `merge_method` value | unknown/unresolved | absence noted, not inferred | Same source as above; the `merge_method` parameter's schema lists only the enum, no `default` field or descriptive sentence |
| `GET /repos/{owner}/{repo}/pulls/{pull_number}/merge` — "Checks if a pull request has been merged... HTTP status... response body is empty"; 204 merged / 404 not merged | explicit fact | sourced | docs.github.com/en/rest/pulls/pulls — structured docs data, `restOperations[title="Check if a pull request has been merged"]` |
| `PUT .../merge-async` and `GET .../merge-async/{uuid}` exist as REST operations | explicit fact (existence only) | sourced (title/path only; body not fetched) | docs.github.com/en/rest/pulls/pulls — structured docs data, `restOperations[].title`/`requestPath` list |
| `mergeable`, `mergeable_state` (example value `"clean"`), `merged`, `merged_by`, `rebaseable`, `draft`, `auto_merge` appear on the pull request resource | explicit fact | sourced (example values only; no field-by-field descriptive text found) | docs.github.com/en/rest/pulls/pulls — structured docs data, `codeExamples[].response.example` |
| Full set/meaning of `mergeable_state` values | unknown/unresolved | not found | Not present in any page fetched today |
| `pull_request` webhook event: full action list (`assigned`, `auto_merge_disabled`, `auto_merge_enabled`, `closed`, `converted_to_draft`, `demilestoned`, `dequeued`, `edited`, `enqueued`, `labeled`, `locked`, `milestoned`, `opened`, `ready_for_review`, `reopened`, `review_request_removed`, `review_requested`, `stacked`, `synchronize`, `unassigned`, `unlabeled`, `unlocked`) | explicit fact | sourced | docs.github.com/en/webhooks/webhook-events-and-payloads#pull_request — structured docs data, `webhooks[name="pull_request"].actionTypes` |
| "This event occurs when there is activity on a pull request... To subscribe to this event, a GitHub App must have at least read-level access for the 'Pull requests' repository permission." | explicit fact | sourced | Same source, `webhooks[name="pull_request"].data.summaryHtml` |
| "For activity related to pull request reviews, pull request review comments... use the `pull_request_review`, `pull_request_review_comment`... events instead." | explicit fact | sourced | Same source, `summaryHtml` |
| Per-action description text and full nested payload schema (e.g. for `opened`/`closed` specifically, and the `pull_request` object's own properties including a `merged` field inside the webhook payload) | unknown/unresolved | not retrievable | Same source page; only the default (`assigned`) action's example was present in the statically-served data — other actions' descriptions/schemas are loaded client-side and were not retrievable via static fetch or WebFetch in today's session |
| `merge_group` object: "A group of pull requests that the merge queue has grouped together to be merged." | explicit fact | sourced | docs.github.com/en/webhooks/webhook-events-and-payloads — structured docs data (found while searching for "merged") |
| "GitHub will merge all these changes into the `base_branch` once the checks required by the branch protections of `base_branch` pass." — the merge queue itself performs the merge, not just sequencing | explicit fact | sourced (re-fetched 2026-08-26 during post-audit fix-up, after the initial guessed URL 404'd) | docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/managing-a-merge-queue |
| Branch protection: "collaborators can only push changes to a protected branch via a pull request that is approved by the required number of reviewers"; error message on pending/rejected reviews | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches (the URL given in the task, ending `.../about-protected-branches` under `enabling-features-for-your-repository`, returned 404; this is the current equivalent page found by searching, per the task's own fallback instruction) |
| Required status checks: "all required status checks must pass before collaborators can merge... successful, skipped, or neutral status" | explicit fact | sourced | Same corrected page |
| Required linear history: "Enforcing a linear commit history prevents collaborators from pushing merge commits to the branch" | explicit fact | sourced | Same corrected page |
| Admin/bypass-role default: "By default, the restrictions of a branch protection rule don't apply to people with admin permissions... or custom roles with the 'bypass branch protections' permission"; organizations can extend restrictions to them | recommended default (source's own "by default" wording) | sourced | Same corrected page |
| "You can't merge a draft pull request." | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/pull-requests/collaborating-with-pull-requests/incorporating-changes-from-a-pull-request/merging-a-pull-request |
| Three merge methods and their effect on history (merge commit "preserves every commit... adds an explicit merge point"; squash "combines all commits... into a single commit"; rebase "adds each commit onto the base branch without a merge commit, for a linear history") | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/pull-requests/collaborating-with-pull-requests/incorporating-changes-from-a-pull-request/about-pull-request-merges |
| Plain "Merge pull request" button described as using a merge commit with `--no-ff` by default (web-UI specific) | explicit fact (web UI only) | sourced (via WebFetch prose extraction) | Same source; not extended by inference to the REST API's own default |
| "Rebase and merge will always update the committer information and create new commit SHAs." | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/pull-requests/collaborating-with-pull-requests/incorporating-changes-from-a-pull-request/merging-a-pull-request |
| Optional branch deletion after merge, framed as tidiness, "not a requirement" | recommended default | sourced (via WebFetch prose extraction) | Same source |
| Web-UI PR creation: write access requirement, base vs. compare branch concept, "Pull requests can only be opened between two different branches," draft vs. ready-for-review creation buttons | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/creating-a-pull-request |
| Repository role required to merge: "Write, Maintain, or Admin roles [can merge]... Read and Triage roles unable to perform this action" | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/organizations/managing-user-access-to-your-organizations-repositories/managing-repository-roles/repository-roles-for-an-organization |
| `gh pr create [flags]`; flags `-B/--base`, `-H/--head`, `-t/--title`, `-b/--body`, `-d/--draft` | explicit fact | sourced (via WebFetch prose extraction) | cli.github.com/manual/gh_pr_create |
| `gh pr merge [<number> \| <url> \| <branch>] [flags]`; flags `-m/-s/-r`, `--auto`/`--disable-auto`, `--admin` ("Bypass merge requirements using administrator privileges"), `-d/--delete-branch` | explicit fact | sourced (via WebFetch prose extraction) | cli.github.com/manual/gh_pr_merge |
| "When Dependabot identifies an outdated dependency, it raises a pull request to update the manifest..."; user "check[s] that your tests pass, review[s] the changelog... and then merge[s] it" (manual merge) | explicit fact | sourced (via WebFetch prose extraction) | docs.github.com/en/code-security/dependabot/dependabot-version-updates/about-dependabot-version-updates |
| GraphQL API exists for managing pull requests (cross-reference only; no mutation name/field confirmed) | explicit fact (existence only) | sourced (existence); unresolved (detail) | webhook page's own `summaryHtml`, "see the GraphQL API documentation... or 'Pulls' in the REST API documentation"; a direct fetch of docs.github.com/en/graphql/reference/mutations did not yield mutation-level detail in today's session (content loads via a separate, non-static data asset) |
| `microsoft/vscode-pull-request-github` — real repository, owner `microsoft`, description "GitHub Pull Requests for Visual Studio Code"; README describes "review and manage" PRs/issues, browsing, in-editor commenting, checkouts — does not explicitly mention "create" or "merge" actions in the fetched excerpt | explicit fact (existence/description); unresolved (create/merge support) | sourced | api.github.com/repos/microsoft/vscode-pull-request-github; raw.githubusercontent.com/microsoft/vscode-pull-request-github/main/README.md |
| schema.org/Person: "A person (alive, dead, undead, or fictional)." | explicit fact | sourced | schema.org/Person |
| schema.org/CreateAction: "The act of deliberately creating/producing/generating/building a result out of the agent." | explicit fact | sourced | schema.org/CreateAction |
| schema.org/UpdateAction: "The act of managing by changing/editing the state of the object." | explicit fact | sourced | schema.org/UpdateAction |
| schema.org/ReviewAction: "The act of producing a balanced opinion about the object for an audience. An agent reviews an object with participants resulting in a review." | explicit fact | sourced | schema.org/ReviewAction |
| schema.org/SoftwareSourceCode: "Computer programming source code..." | explicit fact | sourced | schema.org/SoftwareSourceCode |
| schema.org/MergeAction does not exist | explicit fact (absence) | sourced (404 response) | schema.org/MergeAction — checked and rejected before use, to avoid inventing a grounding term |
| Attempted GITHUB_TOKEN `pull-requests` permission-scope table (for a possible bot/Actions "merge" realization) | unknown/unresolved | not retrievable | docs.github.com/en/actions/security-for-github-actions/security-guides/automatic-token-authentication and .../actions/using-jobs/assigning-permissions-to-jobs — both pages loaded, but the specific permissions table was not present in the content retrieved via WebFetch |
